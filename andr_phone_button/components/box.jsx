import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
  } from 'react-native';

export const Box = () =>{
    return <Text>Helllo this is box</Text>
}